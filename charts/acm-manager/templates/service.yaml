apiVersion: v1
kind: Service
metadata:
  name: {{ include "acm-manager.fullname" . }}
  labels:
    {{- include "acm-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acm-manager.selectorLabels" . | nindent 4 }}
